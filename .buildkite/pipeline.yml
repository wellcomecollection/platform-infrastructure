steps:
  - command: .buildkite/scripts/run_autoformat.py
    label: "autoformat"
  - command:
      pip3 install --user boto3 &&
      python3 builds/copy_docker_images_to_ecr.py
    label: "mirror ECR images"
