{
  "name": "lambdas",
  "version": "1.0.0",
  "description": "Lambda for handling CloudFront invalidation requests",
  "main": "index.js",
  "engines": {
    "node": "12.*.*"
  },
  "license": "MIT",
  "devDependencies": {
    "@aws-sdk/types": "^3.10.0",
    "@types/aws-lambda": "^8.10.73",
    "@types/node": "^14.14.37"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && (cd dist && zip -r sns_handler.zip .)",
    "deploy": "yarn build && node deploy",
    "dockerLoginLocal": "aws ecr get-login-password --region eu-west-1 --profile platform | docker login --username AWS --password-stdin 760097843905.dkr.ecr.eu-west-1.amazonaws.com",
    "dockerBuildLocal": "yarn dockerLoginLocal && docker build . -t weco_cf_lambdas",
    "dockerDeployLocal": "yarn dockerBuildLocal && docker run weco_cf_lambdas -v ~/.aws:/root/.aws yarn deploy"
  },
  "dependencies": {
    "aws-sdk": "^2.881.0"
  }
}
